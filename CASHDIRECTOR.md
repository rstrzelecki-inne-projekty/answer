# Fork Apache Answer dla portalu Pomoc AO AOA (CashDirector)

Gałęzie:
- `main` — czysty upstream (`git fetch upstream && git merge --ff-only upstream/main`). Nigdy nie commitować bezpośrednio.
- `cashdirector` — `main` + nasze łatki. Z niej budowany jest obraz produkcyjny (repo `answers`, `Dockerfile`).

Konwencje łatek:
- jedna funkcjonalność = jeden commit, tytuł z prefiksem `[cd]`; bez poprawek „przy okazji”, bez zmian formatowania cudzych plików;
- przy nowym wydaniu upstreamu: `git rebase v2.x.y` gałęzi `cashdirector`, konflikty tylko w naszych plikach;
- `git format-patch upstream/main..cashdirector` = lista tego, czym się różnimy (miara długu — ma być krótka);
- co się da, oddajemy upstreamowi jako PR; po merge łatka znika stąd;
- każda łatka z logiką w Go ma test jednostkowy (fałszywki repozytoriów, bez bazy — wzór: `internal/service/badge/badge_award_admin_test.go`)
  albo scenariusz w `answers/dev/smoke.sh`; `go test ./internal/... ./pkg/...` musi przechodzić przed tagiem produkcyjnym.

Tagi produkcyjne: `cd-v<wersja upstreamu>-<numer>` (np. `cd-v2.0.2-1`). Obraz w ECR: `answer:<wersja>-cd<numer>`.

Pełne wytyczne: `answers/docs/fork-answer-wytyczne.md`. Zadania: Jira AA-26.

## Łatki na tej gałęzi
| # | Commit | Co | Status upstream |
|---|---|---|---|
| 13 | Konkurs „Pomocnik Automatyzacji” — Punkty Pomocy | silnik PP w `UserService` (`contestItems`) liczony z `activity` (`ActivityRepo.ListByTypesBetween`) za bieżący kwartał: 10 PP rozwiązanie, 5 PP odpowiedź z głosem od kogoś z reputacją ≥100, 4 PP pytanie z głosem, pół stawki za odpowiedź na własne pytanie, sufit 15 PP/odpowiedź, czapka 40% na pytania, wykluczenia ról 2/3 i kont serwisowych (`CONTEST_EXCLUDED_USERNAMES`); sekcja „Ranking do nagrody kwartalnej” na stronie Użytkownicy + `GET /answer/api/v1/contest/my-points` i okno „Moje punkty” (§10.2 regulaminu) | lokalne (regulamin konkursu CashDirector) |
| 12 | Alert o odznace da się zamknąć | `NotificationService.ClearIDUnRead` kasuje wpis z cache alertów (`RemoveBadgeAwardAlertCache`) **przed** sprawdzeniem wiersza powiadomienia — gdy powiadomienie zostało usunięte (np. cofnięta odznaka) albo należy do kogoś innego, stary kod wychodził po cichu z 200 i modal „Gratulacje” wracał przy każdym przeładowaniu; scenariusz w `answers/dev/smoke.sh` | kandydat do PR upstream (błąd upstreamu) |
| 11 | Wizytówka prestiżu przy użytkowniku | serwis `internal/service/user_prestige` (stopień = najwyższa odznaka z grupy „Stopnie”, licznik odznak bez stopni i żółtej kartki, licznik kartek; metadane odznak w cache 10 min, liczniki świeże), zapytanie wsadowe `badgeAwardRepo.BatchUserEarnedCount`, pole `prestige` w `UserBasicInfo`, operatorze listy pytań, komentarzu (+`user_rank`), rankingu `/user/ranking` i wierszach TOP użytkowników; `/user/ranking`: reputacja z `user.rank` bez okna tygodniowego (`userRepo.ListTopByRank`), głosy za całą historię, sortowanie stopień → reputacja, konta serwisowe poza rankingiem (`RANKING_EXCLUDED_USERNAMES`, domyślnie `baza-wiedzy`); UI: komponent `UserPrestige` (pasek plakietek) w `BaseUserCard`, komentarzach, stronie Użytkownicy i kartach panelu kontrolnego; env `PRESTIGE_RANK_GROUP_NAME`, `PRESTIGE_YELLOW_CARD_BADGE_NAME` | lokalne (odznaki „wojskowe” są naszą konwencją) |
| 10 | Wiadomości admina | tabela `admin_message` + serwis `internal/service/admin_message` (rola 2/3), powiadomienie inbox `msg_type=4` (`inbox_type=messages`, `NotificationAdminMessage`, gałąź `AdminMessageObjectType` w `AddNotification`), `read_at` przez hook w `notificationRepo.ClearUnRead/ClearIDUnRead`, API `/answer/api/v1/admin-message` (+`/page`), UI: `MessageUserButton` (pytanie, odpowiedź, komentarz), kategoria „Wiadomości” w Powiadomieniach, Admin → Społeczność → Wiadomości admina | PR https://github.com/apache/answer/pull/1627 (otwarty 2026-09-16, gałąź `feat/admin-messages`, bez wpisu do logu aktywności); po merge usunąć łatkę przy rebase |
| 9 | Kwarantanna przy edycji komentarza | `CommentService.UpdateComment` przepuszcza nową treść przez `ReviewService.AddCommentReview` (jak przy dodaniu; upstream sprawdza tylko nowe komentarze) → status 11 + wpis w `review`; `status` w odpowiedziach add/update, UI chowa komentarz i pokazuje „czeka na zatwierdzenie” | PR https://github.com/apache/answer/pull/1626 (otwarty 2026-09-16, gałąź `fix/review-comment-edits`); po merge usunąć łatkę przy rebase |
| 8 | Dashboard: aktywność dzienna + TOP 50 użytkowników | `GET /answer/admin/api/activity-log/daily` (agregacja w strefie przeglądarki) + karta `Admin/Dashboard/components/DailyActivity` — 14 dni × (pytania, odpowiedzi, komentarze, review, odznaki, wyświetlenia), liczby linkują do logu z filtrem; `GET …/activity-log/top-users?from&to&limit` + karta `TopUsers` (zakres dat, ranking = pytania+odpowiedzi+komentarze) | PR https://github.com/apache/answer/pull/1625 (stacked na #1624, gałąź `feat/dashboard-daily-activity`) |
| 7 | Sortowanie „Najwięcej wyświetleń” | `order=views` na liście pytań (`question.view_count DESC`), przycisk w `QUESTION_ORDER_KEYS`, i18n `question.views` | PR https://github.com/apache/answer/pull/1623 (otwarty 2026-09-16, gałąź `feat/most-viewed-order`); po merge usunąć łatkę przy rebase |
| 6 | Log aktywności społeczności | tabela `activity_log` + `internal/service/activity_log` (odbiorca kolejki zdarzeń; kolejka obsługuje wielu odbiorców), hooki: logowanie (`userRepo.UpdateLastLoginDate`), odznaki, review, `ChangeUserRank` (kontekst `WithRankContext`), role/blokady; admin API `/answer/admin/api/activity-log` (+`/actions`, `/export` TSV), beacon `POST /answer/api/v1/activity-log/view`, cron retencji `ACTIVITY_LOG_PAGEVIEW_DAYS` (90); UI Admin → Społeczność → Log aktywności | PR https://github.com/apache/answer/pull/1624 (otwarty 2026-09-16, gałąź `feat/activity-log`; bez hooka w `AdminAward` — upstream nie ma łatki 3 — i bez panelu Nagrody); po merge usunąć łatkę przy rebase |
| 5 | Przeładowanie przy nieaktualnym bundle | `ui/src/router/index.tsx`: nieudany `import()` strony (stary chunk po wdrożeniu) przeładowuje kartę raz (`sessionStorage cd_stale_chunk_reload`), zamiast ekranu „50X”; `internal/router/ui.go`: brakujący `/static/*` → 404 zamiast `index.html` (Caddy robi to samo na warstwie wyżej) | PR https://github.com/apache/answer/pull/1619 (otwarty 2026-09-15, gałąź `fix/stale-chunk-reload`); po merge usunąć łatkę przy rebase |
| 4 | Głos w dół i reputacja za komentarze | typy aktywności `comment.vote_down/voted_up/voted_down` (config ID 901–903; na istniejącej bazie seed przez `answers/17-configure-reputation.sh`), `getActivities` rozróżnia up/down dla komentarza, `vote_status` w odpowiedzi komentarza, kciuk w dół w `Comment/ActionBar`, licznik z odpowiedzi serwera | lokalne (AA-35); upstream ma tylko `comment.vote_up`=0 |
| 3 | Ręczne odznaki (admin) | API `POST/DELETE /answer/admin/api/badge/award` + komponent `AwardBadgeButton` (profil→Odznaki z cofaniem, strona odznaki, Admin→Odznaki, lista pytań, pytanie/odpowiedzi); powiadomienie w dzwonku przez `BadgeAwardService.Award` | kandydat do PR upstream (AA-34) |
| 2 | Content-Type uploadów | `AvatarThumb` ustawiał `image/<ext>` dla każdego pliku z `/uploads` (`image/svg` → przeglądarki nie renderują SVG); teraz `mime.TypeByExtension` z fallbackiem | PR https://github.com/apache/answer/pull/1618 (otwarty 2026-09-15); po merge usunąć łatkę przy rebase |
| 1 | i18n pl_PL | `Polski` w `language_options`, pełne `pl_PL.yaml` (upstream + 393 uzupełnienia), klucze odznak `badge.aoa.*` w en/pl | tłumaczenie do zgłoszenia przez Crowdin; klucze odznak są nasze (nie do upstreamu) |
